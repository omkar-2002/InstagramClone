export const isAuthLoadingSelector = (state) => state.auth.isAuthLoading

export const isAuthenticatedSelector = (state) => state.auth.isAuthenticated

export const authStatusSelector = (state) => state.auth.authStatus

export const authUserSelector = (state) => state.auth.user
